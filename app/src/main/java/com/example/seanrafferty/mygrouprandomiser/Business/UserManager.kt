package com.example.seanrafferty.mygrouprandomiser.Business

import android.content.Context

/**
 * Business logic for managing firebase data for logged in users including if user is new or additional user content
 * @param context : current activity context
 */
class UserManager (val context: Context?)
{
}